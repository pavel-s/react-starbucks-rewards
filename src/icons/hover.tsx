import styled from 'styled-components/macro';

export const Hover = styled.circle`
  transition: all 0.2s ease-out;
  transform: scale(0.4);
  transform-origin: center;
  opacity: 0.1;
  fill: transparent;
`;
